l1:dict<nonexisting_type, top> := {5:"a",(10 * 10):"b", (16 - 5):"c", nonexisting_variable:"d", 64:"e"};
l2:seq<int> := [1,2,3,4,5];
 
s1:string := "";
s2:string := "hello";

main
{

  b:bool := len(thirdlist) = len(l1);


  if (len(newlist) <= len(anotherlist)) then
     newlist := newlist + anotherlist[0];
  else 
     nelist := newlist - newlist[0];
  fi
  
  return;
}
